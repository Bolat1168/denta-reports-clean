const admin = require("firebase-admin"); const nodemailer = require("nodemailer"); if (!admin.apps.length) { admin.initializeApp({ credential: admin.credential.applicationDefault() }); } const db = admin.firestore(); module.exports = async (req, res) => { try { const yesterday = new Date(); yesterday.setDate(yesterday.getDate() - 1); const dateStr = yesterday.toISOString().split("T")[0]; const slots = await db.collection("slots").where("date", "==", dateStr).get(); res.status(200).json({ success: true, date: dateStr, total: slots.size }); } catch (error) { res.status(500).json({ success: false, error: error.message }); } };
